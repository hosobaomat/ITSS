class Userresponse {
  int? userid;
  String? username;
  String? email;
  String? fullName;
  String? role;
  DateTime? createdAt;
  DateTime? updatedAt;

  Userresponse(this.userid, this.username, this.email, this.fullName, this.role,
      this.createdAt, this.updatedAt);
  //Set<ShoppingListItemRequest> items = new HashSet<>();
}
